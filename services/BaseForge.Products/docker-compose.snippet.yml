# Bu bloğu kök docker-compose.yml'a ekleyin (postgres servisinin yanına).
services:
  products-service:
    build: .
    environment:
      ConnectionStrings__Default: "Host=postgres;Port=5432;Database=products_db;Username=baseforge;Password=change_me"
    depends_on:
      postgres:
        condition: service_healthy
