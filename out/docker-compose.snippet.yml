# Bu bloğu kök docker-compose.yml'a ekleyin (postgres servisinin yanına).
services:
  orders-service:
    build: .
    environment:
      ConnectionStrings__Default: "Host=postgres;Port=5432;Database=orders_db;Username=baseforge;Password=change_me"
    depends_on:
      postgres:
        condition: service_healthy
